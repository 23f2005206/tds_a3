name: CI Status Check

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # This allows you to run it manually!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a simple script
        run: echo "Everything is looking good!"
